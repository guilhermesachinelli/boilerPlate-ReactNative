import { StyleSheet } from "react-native";
const styles = StyleSheet.create({
    container: {
        flex: 1,
        alignItems: "center",
    },
    text : {
        fontSize: 20,
        margin: 10,
    },
    tittle: {
        fontSize: 30,
        fontWeight: "bold",
    },
    });
    export default styles;