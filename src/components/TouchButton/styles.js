import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  title: {
    fontSize: 24,
  },
  button: {
    marginTop: 20,
    padding: 10,
    borderRadius: 5,
  },
});

export default styles;